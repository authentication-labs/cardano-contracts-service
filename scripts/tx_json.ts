// In order to run this script, you need to have the `@lucid-evolution/lucid` package installed.

// import * as lucid from "@lucid-evolution/lucid";

// const decoder = new TextDecoder("utf-8");
// const data = Deno.readFileSync("tmp/tx1.signed");
// const bytesStr = decoder.decode(data);
// console.log();

// const tx = lucid.CML.Transaction.from_cbor_hex(bytesStr);
// console.log(tx.body().to_json())
